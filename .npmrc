# NPM Configuration for multisync library

# Package access (public by default)
access=public

# Registry (use default npm registry)
registry=https://registry.npmjs.org/

# Package type (ES modules)
type=module

# Git hooks (optional - uncomment if you want to use them)
# pre-commit
# pre-push

# Package publishing settings
# tag=latest
# otp=

# Security settings
audit=false
fund=false

# Development settings
save-exact=false
save-prefix=^
